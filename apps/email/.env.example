# Direct PostgreSQL connection for migrations, separate from the Worker binding.
# Local development only. For PlanetScale, use the primary's direct connection
# on port 5432 and sslmode=verify-full with a role allowed to create the mail schema.
DATABASE_URL=postgres://postgres:postgres@127.0.0.1:5434/bezalel_email_dev
