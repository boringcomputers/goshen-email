HOST=127.0.0.1
PORT=3031
DASHBOARD_PUBLIC_URL=http://127.0.0.1:3031
DASHBOARD_PASSWORD=replace-with-an-independent-random-value-at-least-32-characters
MAIL_WORKER_URL=https://your-email-worker.example.workers.dev
MAIL_API_TOKEN=replace-with-the-worker-platform-token
# When using a reverse proxy, trust its connection IP and configure it to
# overwrite X-Real-IP with the client's IP. Untrusted headers are ignored.
# DASHBOARD_TRUSTED_PROXY_IPS=127.0.0.1
# Public accounts: choose account mode, set the matching backend AUTH_PUBLIC_URL,
# and share an independent proxy secret between dashboard and email Worker.
# DASHBOARD_AUTH_MODE=account
# AUTH_PROXY_SECRET=replace-with-an-independent-random-value-at-least-32-characters
