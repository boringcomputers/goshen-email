import Root, { badgeVariants } from "./badge.svelte";
import type { BadgeProps, BadgeSize, BadgeVariant } from "./badge.svelte";

export {
	Root,
	Root as Badge,
	badgeVariants,
	type BadgeProps,
	type BadgeSize,
	type BadgeVariant,
};
